<div class="jumbotron">
    <h1 class="display-4">Bem vindo ao primeiro blog do Renan_xD :D</h1>
    <p class="lead">Quem diria que um diria chegaria</p>
    <hr class="my-4">
    <p>Perseverança e autoconfiança sempre!</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta agora mesmo!</a>
    </p>
</div>

<hr>

<h2>Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
        </div>
</div>
{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}
